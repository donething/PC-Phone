<resources>
    <!-- Base application theme. -->
    <style name="Theme.PCPhone" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green_500</item>
        <item name="colorPrimaryVariant">@color/green_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
        <!-- 不能用"?android:attr/windowBackground"，会导致选项页中点击 EditTextPreference 时崩溃-->
        <!--  报错"Can't find ColorStateList from drawable resource ID #0x7f08003c"。不知原因 -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.PCPhone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>